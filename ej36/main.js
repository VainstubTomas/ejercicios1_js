//Pide al usuario que ingrese dos números utilizando prompt y muestra la suma de estos números en una alerta.

let numeroA = parseInt(prompt("Ingrese un numero entero"));
let numeroB = parseInt(prompt("Ingrese otro numero entero"));

let suma = (numeroA + numeroB);
alert(("La suma de los dos numeros arroja: ") + suma);